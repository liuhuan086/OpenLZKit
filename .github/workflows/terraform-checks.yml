name: terraform-checks

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  static-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: |
          echo "Add terraform fmt, validate, tflint, checkov/tfsec and conftest here."
          find . -maxdepth 4 -type f | sort
