# AWS Departments Example

This example validates the department management contract without real
organization ids. It shows department OUs, department administrator roles,
department policy attachments and department Tag Policy baselines.

Do not run `apply` from this example. Use `live/15-departments` with reviewed
department requests and short-lived credentials from a sandbox management
account.
