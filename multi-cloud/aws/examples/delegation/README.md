# AWS Delegation Example

This example validates delegated administrator and AWS RAM sharing contracts
with placeholder account, OU and resource ARNs.

Do not run `apply` from this example. Use `live/55-delegation` from a sandbox
management account with reviewed service principals and share targets.
