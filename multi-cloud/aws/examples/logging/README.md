# AWS Logging Example

This example validates the organization CloudTrail and log archive contract
with a placeholder bucket name. Do not run `apply` until the bucket name,
KMS policy, CloudWatch role and retention settings are reviewed for a sandbox
log archive account.
