# AWS Identity Center Example

This example validates IAM Identity Center permission set and account
assignment contracts with placeholder instance, identity store, group and
account ids.

Do not run `apply` from this example. Use `live/25-sso` with real Identity
Center instance metadata and reviewed group-to-account assignments.
