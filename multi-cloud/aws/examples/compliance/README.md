# AWS Compliance Example

This example validates AWS Config, Security Hub and GuardDuty runtime
compliance contracts with placeholder organization admin ids and role ARNs.

Do not run `apply` from this example. Use `live/45-compliance` from a sandbox
security/compliance account with reviewed admin account ids, Config roles and
delivery buckets.
