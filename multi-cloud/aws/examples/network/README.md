# AWS Network Example

This example validates a VPC baseline with public/private/isolated subnets,
explicit route tables, a NAT gateway, interface/gateway endpoints and VPC Flow
Logs using placeholder destinations.

Do not run `apply` from this example. Use `live/30-network` with reviewed CIDR
ranges, AZ layout, flow log destinations and endpoint policies.
