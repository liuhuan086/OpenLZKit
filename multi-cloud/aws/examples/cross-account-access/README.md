# AWS Cross-account Access Example

This example validates cross-account IAM role, OIDC and AWS RAM contracts with
placeholder account ids and resource ARNs.

Do not run `apply` from this example. Use `live/24-cross-account-access` in a
sandbox target account with reviewed principals, ExternalId values and resource
share targets.
