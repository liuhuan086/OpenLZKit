# AWS FinOps Example

This example validates budgets, anomaly detection and cost category contracts
with placeholder email subscribers and linked-account names.

Do not run `apply` from this example. Use `live/60-finops` with reviewed budget
limits, alert recipients and cost allocation rules.
