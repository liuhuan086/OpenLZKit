# AWS Verified Path Preflight Summary

Date: `2026-06-16`
Region: `us-east-1`
Conclusion: `blocked-before-sandbox-apply`

This report is generated by `tools/aws_verified_path.py`. It is safe to commit only after reviewing the sanitized output.

## Local Checks

| Check | Status | Sanitized output |
|---|---|---|
| `terraform version` | `pass` | Terraform v1.15.2<br>on darwin_arm64 |
| `conftest version` | `pass` | Conftest: dev<br>OPA: 1.15.2 |
| `static release gate` | `pass` | Static release gate passed |
| `terraform fmt` | `pass` | - |
| `aws conftest verify` | `pass` | 17 tests, 17 passed, 0 warnings, 0 failures, 0 exceptions, 0 skipped |
| `alicloud conftest verify` | `pass` | 10 tests, 10 passed, 0 warnings, 0 failures, 0 exceptions, 0 skipped |
| `checkov terraform scan` | `pass` | terraform scan results:<br><br>Passed checks: 271, Failed checks: 0, Skipped checks: 0 |
| `tflint recursive scan` | `missing-optional` | tflint not found |

## AWS Readiness

| Check | Status | Sanitized output |
|---|---|---|
| `aws cli` | `missing` | aws CLI not found on PATH |
| `aws sts get-caller-identity` | `not-run` | requires aws CLI and short-lived credentials |
| `aws organizations describe-organization` | `not-run` | requires aws CLI and management/delegated credentials |

## Apply Decision

No Terraform apply was executed by this helper. A real AWS sandbox apply requires:

- AWS CLI installed.
- Short-lived IAM Identity Center or OIDC/STS credentials.
- Confirmed sandbox Organization scope.
- Manual approval for Organizations, SCP, delegated admin, logging retention and account vending changes.

## Blockers

- aws cli: aws CLI not found on PATH
- aws sts get-caller-identity: requires aws CLI and short-lived credentials
- aws organizations describe-organization: requires aws CLI and management/delegated credentials
